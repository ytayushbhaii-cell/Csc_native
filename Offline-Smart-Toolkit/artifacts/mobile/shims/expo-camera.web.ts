/**
 * expo-camera — web stub.
 * Scanner screens catch null CameraView and show the "Camera not available" UI.
 */
export const CameraView           = null;
export const Camera               = null;
export const useCameraPermissions = () => [null, async () => {}] as const;
export default null;

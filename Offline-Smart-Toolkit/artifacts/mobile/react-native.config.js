/**
 * React Native CLI configuration.
 * Tells the CLI where to find the project and dependencies.
 */
module.exports = {
  project: {
    android: {
      sourceDir: './android',
    },
  },
  assets: [
    './android/app/src/main/assets/fonts/',
  ],
};
